import EmployeeDashboardPage from './EmployeeDashboardPage.jsx';

/** Kissflow custom component: employee view without outer shell (use with `ProjectTrackerShell` or embed context). */
export default function ProjectEmployeeDashboardPage() {
  return <EmployeeDashboardPage useLayout={false} />;
}
