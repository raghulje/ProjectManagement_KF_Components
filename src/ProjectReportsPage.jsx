import ReportsPage from './ReportsPage.jsx';

/** Kissflow custom component: reports view without outer shell. */
export default function ProjectReportsPage() {
  return <ReportsPage useLayout={false} />;
}
