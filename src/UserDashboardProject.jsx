import UserDashboardPage from './UserDashboardPage.jsx';

/** Kissflow custom component entry — user-scoped project dashboard. */
export default function UserDashboardProject() {
  return <UserDashboardPage useLayout={false} />;
}
