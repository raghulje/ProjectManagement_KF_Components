import ProjectTrackerShell from './ProjectTrackerShell.jsx';

/** Kissflow entry — mounts SPA at /sales-dashboard */
export default function LeadManagementProject() {
  return <ProjectTrackerShell initialRoute="/sales-dashboard" />;
}
